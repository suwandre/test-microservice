{
  "name": "test-microservice",
  "version": "1.0.0",
  "scripts": {
    "docs:generate": "swagger-gen -c swagger-config.json",
    "docs:serve": "node swagger-server.js",
    "docs:build-and-serve": "npm run docs:generate && npm run docs:serve"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.1"
  }
}
