{
  "name": "test-microservice",
  "version": "1.0.0",
  "scripts": {
    "docs:generate": "swagger-gen -c swagger-config.json",
    "docs:serve": "node swagger-server.js",
    "api:start": "node app.js",
    "dev": "npm run api:start",
    "docs:full": "npm run docs:generate && npm run docs:serve"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.1"
  }
}
